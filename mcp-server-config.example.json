{
  "mcpServers": {
    "supabase": {
      "command": "bash",
      "args": [
        "-lc",
        "set -euo pipefail; if [ -f .env ]; then source .env; fi; if [ -f .env.local ]; then source .env.local; fi; : \"${NEXT_PUBLIC_SUPABASE_URL:?Missing NEXT_PUBLIC_SUPABASE_URL}\"; : \"${SUPABASE_MCP_API_KEY:?Missing SUPABASE_MCP_API_KEY}\"; API_URL=\"${NEXT_PUBLIC_SUPABASE_URL%/}/rest/v1\"; exec npx -y @supabase/mcp-server-postgrest --apiUrl \"$API_URL\" --apiKey \"$SUPABASE_MCP_API_KEY\" --schema public"
      ]
    },
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"]
    },
    "stripe": {
      "command": "bash",
      "args": [
        "-lc",
        "if [ -f .env ]; then source .env; fi; if [ -f .env.local ]; then source .env.local; fi; if [ -z \"${STRIPE_SECRET_KEY:-}\" ]; then echo 'Missing STRIPE_SECRET_KEY' 1>&2; exit 1; fi; exec npx -y @stripe/mcp --tools=all --api-key=\"$STRIPE_SECRET_KEY\""
      ]
    }
  }
}
